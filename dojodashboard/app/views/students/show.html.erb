<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1><%=@dojo.branch%>'s <%=@student.fname%> <%=@student.lname%></h1>
        <h4>Email:</h4> <p><%=@student.email%></p>
        <h3>Cohort</h3>
        <table>

        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <% @cohort.each do |x| %>
            <tr>
                <td><%= x.fname %></td>
                <td><%= x.lname %></td>
                <td><%= x.email %></td>

        </tr>
        <%end%>

</table>

<a href = '/'>Home</a> <a href = "/dojos/<%=@dojo.id%>/students/<%=@student.id%>/edit">Edit</a>
    </body>
</html>
