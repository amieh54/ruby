<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
<h3>User <%=@user.id%></h3>
<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <p>Name: <%=@user.name%> </p>

    </body>
</html>
